name = "isla-lyrics-broker"
main = "src/index.ts"
compatibility_date = "2026-09-13"
workers_dev = false

[observability]
enabled = true

# Production release setup stays incomplete until Isla has a signed provider
# agreement. Add rate-limit and analytics bindings in Cloudflare, then set the
# installation-token secret with Wrangler; never commit it.
